#include <bits/stdc++.h>
using namespace std;
static map<int, map<int,int> > a;
static int n;

int main(){
string str="abcde";
str.resize (str.size () - 1);
str.resize (str.size () - 1);
cout<<str;
  return 0;
}
